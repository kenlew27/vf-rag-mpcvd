"""Infrastructure utilities for embedding, retrieval, and database access.

Full implementations are deployed on the production environment and require
LanceDB indices, BigQuery datasets, and API credentials. This package
provides the public interface so that imports resolve for code review.
"""
